/**index.wxss**/
.userinfo {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.userinfo-avatar {
  width: 128rpx;
  height: 128rpx;
  margin: 20rpx;
  border-radius: 50%;
}

.userinfo-nickname {
  color: #aaa;
}

.usermotto {
  margin-top: 200px;
}

.homeContent{
  height: 100vh;
  overflow: hidden;
  padding-bottom:calc(100rpx + env(safe-area-inset-bottom) / 2);
}

.mainContent{
  flex:1;
}

.footContent{
  flex-shrink: 0;
}